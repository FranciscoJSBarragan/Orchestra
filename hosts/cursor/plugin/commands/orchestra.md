---
name: orchestra
description: Start Orchestra in this Cursor chat. Use only for an explicit /orchestra or $orchestra invocation.
---

Activate Orchestra in the current Cursor chat. Read the `orchestra` skill and
the Cursor spawn adapter at
`${ORCHESTRA_HOME:-$HOME/.orchestra}/hosts/cursor/spawn.md`. Do not mix Codex
spawn tools. Recommend `standard`. Recommend `minimal` when cost or speed is
the priority. Unassigned Cursor `critical` is `blocked`.
