interface:
  display_name: "Orchestra Reviewer Role"
  short_description: "Independent review, standalone or phased"
  default_prompt: "Use $orchestra-role-reviewer to independently review one bounded target standalone or under the Orchestra reviewer role contract."
