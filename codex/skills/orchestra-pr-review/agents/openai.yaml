interface:
  display_name: "Orchestra PR Review"
  short_description: "Observe and converge a PR until clean"
  default_prompt: "Use $orchestra-pr-review so the root observes this PR directly, reviews feedback independently, and converges it until clean."
