interface:
  display_name: "Orchestra Verifier Role"
  short_description: "Source-read-only verification role contract"
  default_prompt: "Use $orchestra-role-verifier to run source-read-only verification and report observed evidence under the Orchestra verifier role contract."
