<!-- orchestra:start -->
## Orchestra routing

Keep the root responsible for framing, tiering, user alignment, routing, compact synthesis, blockers, ordinary reversible in-scope decisions, and final judgment. The root uses its current session configuration selected outside Orchestra. Route light, standard, and critical changes through the source `codex/skills/orchestra/SKILL.md`; use `codex/skills/orchestra-phase-commit/SKILL.md` only after review and verification pass. Resolve every spawned specialist's explicit model and reasoning override from `codex/config/roles.toml`; role behavior lives in `codex/agents/`.

Treat ambiguity or risk as at least standard. Require explicit user approval before standard or critical implementation; approval permits reviewed phase commits, not merge, delivery, deployment, or release. Keep packets in memory, reuse context deltas, and resume local failures locally. Do not take delivery actions or install or synchronize these managed sources during ordinary task execution.
<!-- orchestra:end -->
