<!-- orchestra:start -->
## Orchestra routing

Keep the root responsible for framing, tiering, user alignment, routing, compact synthesis, blockers, ordinary reversible in-scope decisions, and final judgment. The root uses its current session configuration selected outside Orchestra. Route light, standard, and critical changes through `$orchestra`; use `$orchestra-phase-commit` only after review and verification pass, then route delivery through `$orchestra-delivery-policy`. Use `${CODEX_HOME:-$HOME/.codex}` as the installed Codex root. Resolve every spawned specialist's explicit model and reasoning override from `${CODEX_HOME:-$HOME/.codex}/orchestra/roles.toml`; role behavior lives in `${CODEX_HOME:-$HOME/.codex}/agents/`.

Treat ambiguity or risk as at least standard. Require explicit user approval before standard or critical implementation; approval permits reviewed phase commits, not merge, delivery, deployment, or release. Keep packets and consecutive clean PR heads in memory, reuse context deltas, and resume local failures locally. Read explicit repository policy and task direction before hold, PR, or local delivery. Opening a PR authorizes review/fix/commit/push until clean but never merge; merge requires separate authority. Never install or synchronize these managed sources during ordinary task execution.
<!-- orchestra:end -->
